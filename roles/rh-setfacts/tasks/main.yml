---
#- name: define first controller fact
#  set_fact: first_controller={{ groups['controller'][0] }}
#  tags: setfacts
#  always_run: yes
#
- name: set fact about primary ip address
  set_fact: primary_ipaddr={{ hostvars[inventory_hostname][ "ansible_" + mgmt_interface ].ipv4.address }}
  tags: setfacts
  always_run: yes

- name: debug interface ip address
  debug: var=primary_ipaddr

- name: Get Current DC
  shell: pcs status cluster | awk '/^ Current DC:/ {print $3}'
  register: pacemaker_dc
  tags:
    - pacemaker
    - pacemaker-haproxy

